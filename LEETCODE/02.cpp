#include <iostream>
#include <vector>

using namespace std;
int main()
{
    int qtd;
    int balon = 0;
    int k = 0;
    int pivot = 0;

    vector<int> array(qtd);

    // vector<vector<int>> matriz(qtd, vector<int>(qtd));

    while (1)
    {
    }

    return 0;
}